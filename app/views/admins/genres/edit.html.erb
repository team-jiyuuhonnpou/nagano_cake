<h2>ジャンル編集</h2>

<%= form_with (model: Genre, local: true) do |f| %>
<div>
  ジャンル
    <%= f.text_field :name %>
    <%= f.radio_button :is_active, true %>
    <%= f.lavel :is_active, "有効", {value: :true} %>
    <%= f.radio_button :is_active, false %>
    <%= f.lavel :is_active, "無効", {value: :false} %>
    <%= f.submit "変更を保存する", class: "btn btn-info" %>
</div>
<% end %>